security:
  basic:
    enabled: false

zuul:
  add-proxy-headers: true
  ignored-services: "*"
  routes:
    employees:
      path: /api/employees/**
      url: cts-workshop-spring-employees.cfapps.io
      stripPrefix: false
