security:
  basic:
    enabled: false

zuul:
  add-proxy-headers: true
  ignored-services: "*"
  routes:
    employees:
      path: /api/employees/**
      url: https://cts-workshop-spring-employees.cfapps.io
      stripPrefix: false
